generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("MYSQL_DATABASE_URL")
}

enum Status {
  incomplete
  authentic
  blocked
  rejected
}


//Initial commit
